# Work Pattern Analyzer 2025 📊

Система глубокой аналитики рабочей продуктивности. Проект обрабатывает данные об активностях, классифицирует их по категориям (Deep Work, Meetings и др.), кластеризует рабочие дни по стилям нагрузки и визуализирует результат в интерактивном дашборде.



## Основные возможности
* **Автоматическая классификация:** Разделение задач на "глубокую работу", "встречи" и "обучение".
* **Кластеризация (ML):** Определение стиля дня (например, "Пик продуктивности" или "Операционка").
* **Интеллектуальные метрики:** Расчет Maker's Index, Deep Work Ratio и индекса фрагментации.
* **Интерактивный UI:** Дашборд на Dash с тепловыми картами и детализацией по категориям.

---

## Конфигурация и логика обработки (`src/config.py`)
1. Маппинг столбцов (Column Mapping)
Система автоматически переименовывает кириллические заголовки из Excel в стандартизированные атрибуты:

* `RAW_COLUMNS`: список ожидаемых столбцов в исходном файле (Неделя, Дата, С, По, Часы и т.д.).
* `COLUMN_MAPPING`: словарь преобразования (например, С → start_time, Описание → description).
* `PROCESSED_COLUMNS`: финальный набор признаков для анализа

2. Категоризация активностей (`CATEGORY_MAP`)
Каждая задача из колонки NEW_Тип Активности классифицируется по одной из трех ключевых групп:
* Deep Work
* Training
* Meeting
* Other
При необходимости необходимо дополнить или изменить структуру мэппинга

3. Признаки кластеризации (`CLUSTER_FEATURES`)
Для определения стиля рабочего дня используются два ключевых признака:
* focus_density: концентрация задач в течение дня.
* session_depth: продолжительность и непрерывность сессий.
Расчет важности признаков регулируется через `FEATURE_WEIGHTS`, где по умолчанию установлена равная значимость (0.5 / 0.5).

4. Весовые коэффициенты (`WEIGHTS`)
При расчете финальных индексов продуктивности категории имеют разный приоритет, указанный в словаре

## Требования к данным
При стандартных настройках система ожидает файл:
* **Путь:** Папка `data/timesheet.xlsx`.
* **Формат:** Excel (`xlsx`).

## Как запустить

### 1. Подготовка окружения
Требуется Python 3.9+.
```
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt
```
## 2. Запуск
```
python main.py
```
После завершения расчетов скрипт автоматически откроет вкладку в браузере по адресу: `http://127.0.0.1:8050`

## Структура проекта
`main.py` — главный диспетчер: запускает пайплайны и визуализацию.
`app.py` — код интерфейса Dash и логика графиков.
`dashboard.py` — расчет аналитических метрик и агрегатов.
`src/config.py` — настройки категорий, весов кластеризации и путей.
`src/pipeline.py` — координация этапов обработки данных.
`src/features.py` — математическая трансформация временных рядов в признаки.